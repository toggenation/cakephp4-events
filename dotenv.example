# Environment for docker-compose.yaml
# copy this to .env and edit
BASE_PORT=50
FORWARD_SFTP_PORT=${BASE_PORT}10
FORWARD_REDIS_PORT=${BASE_PORT}15
FORWARD_NGINX_PORT=${BASE_PORT}20
FORWARD_CUPS_PORT=${BASE_PORT}25
FORWARD_DB_PORT=${BASE_PORT}30
FORWARD_MAILHOG_PORT=${BASE_PORT}35
FORWARD_MAILHOG_DASHBOARD_PORT=${BASE_PORT}40
FORWARD_POSTGRES_PORT=${BASE_PORT}45

# mysql
MYSQL_DATABASE=dev
MYSQL_USER=dev
MYSQL_PASSWORD=dev
MYSQL_ROOT_PASSWORD=${MYSQL_PASSWORD}

# postgres
POSTGRES_PASSWORD=${MYSQL_PASSWORD}
POSTGRES_USER=${MYSQL_USER}
POSTGRES_DB=${MYSQL_DATABASE}
